import { RegionFiltersPipe } from './region-filters.pipe';

describe('RegionFiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new RegionFiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
